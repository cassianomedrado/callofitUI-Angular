import { StatusChamadoTextoPipe } from './status-chamado-texto.pipe';

describe('StatusChamadoTextoPipe', () => {
  it('create an instance', () => {
    const pipe = new StatusChamadoTextoPipe();
    expect(pipe).toBeTruthy();
  });
});
